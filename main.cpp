#include <bits/stdc++.h>
#include <stack.h>

using namespace std;

int main(){
    Stack sta;

    if(sta.empty()){
        cout << "Stack starts off empty!" << endl;
    }

    sta.push("Hello");
    sta.push("World");
    sta.push("!");

    for(int i = 0; i < 10000; i++){
        
    }
}