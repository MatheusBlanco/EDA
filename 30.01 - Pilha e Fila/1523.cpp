#include <bits/stdc++.h>

using namespace std;

int main(){
    int n, k;

    cin >> n >> k;
    while(n != 0 && k != 0){
        if(n >= 3 && n <= 10004 && k >= 1 && k <= 1003){
            int ci, si, i;
            cin >> ci >> si;
            if(ci >= 1 && si <= 100005){
                
            } else return 0;
        } else return 0;
    }

    return 0;
}