//modelo 
#include <bits/stdc++.h>
#define debug(x) cerr << #x << ": " << x << endl;

using namespace std;

int main(){
    return 0;
}

//encontrar o elemento dentro do array. false = array+n
int  * pos = find(op, op+n, quer[i]);
//Se encontrou o elemento
if(pos != op+n)
//Posição do elemento
pos - op + 1

//Subtrair uma string
x = a.substr(a.size() - b.size());

//apaga espaços
elts.erase(remove(elts.begin(), elts.end(), ' '), elts.end());

//EOF
while(scanf("%lld%lld", &a, &b) != EOF)

//ordenação decrescente
sort(a, a+n, greater<int>());

//struct
typedef struct Time

//scanf
scanf("%d", &nc);

//par 
//simplificando o chamado
using si = pair<int,string>;
//criando um vector com o par dentro
vector<si> func;
//dando push 
func.push_back(make_pair(idade, nome));
//ordenando
sort(func.begin(), func.end());
//for each
for(auto value : func) {
    cout << value.second<< endl;
}

//map
map<string, int> ende;
//new el
estados["Mato Grosso"] = 66
//remover el
estados.erase("Sergipe");
// Procura de itens
if (estados.find("Amazonas") != estados.end()) {
    cout << "Estado encontrado!\n";
}
// Iteração sobre todos os itens
for(auto chave_valor: estados)
    cout << chave_valor.first << " => " << chave_valor.second << "\n";